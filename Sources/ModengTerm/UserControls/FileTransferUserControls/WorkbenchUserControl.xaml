<UserControl x:Class="ModengTerm.UserControls.FileTransUserControls.WorkbenchUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ModengTerm.UserControls.FileTransUserControls"
             xmlns:converters="clr-namespace:ModengTerm"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>

        <converters:FsOperationTypeNameConverter x:Key="FsOperationTypeNameConverter"/>
        <converters:ProcessStatesNameConverter x:Key="ProcessStatesNameConverter"/>

        <Style x:Key="StyleTextBlockLocal" TargetType="{x:Type TextBlock}">
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="200" MinHeight="150" MaxHeight="300"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Grid.RowSpan="999" Grid.Column="0" Grid.ColumnSpan="999" Background="#F0F0F0"/>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <local:FileSystemTreeUserControl Grid.Column="0" DataContext="{Binding Path=ClientFsTree}" Margin="3,3,0,3"
                                             EnterDirectory="FileSystemTreeUserControl_EnterDirectory"
                                             TransferFile="FileSystemTreeUserControl_StartTransfer"/>

            <GridSplitter Grid.Column="1" ShowsPreview="True" ResizeDirection="Columns" Background="Transparent"
                          HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>

            <local:FileSystemTreeUserControl Grid.Column="2" DataContext="{Binding Path=ServerFsTree}" Margin="0,3,3,3"
                                             EnterDirectory="FileSystemTreeUserControl_EnterDirectory"
                                             TransferFile="FileSystemTreeUserControl_StartTransfer"/>

        </Grid>

        <GridSplitter Grid.Row="1" ShowsPreview="True" ResizeDirection="Rows" Background="Transparent"
                          HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>

        <Grid Grid.Row="2">
            <DataGrid x:Name="DataGridFileStatusList" ItemsSource="{Binding Path=FileStatus}" 
                      Style="{StaticResource ResourceKey=StyleDataGrid}">

                <DataGrid.RowStyle>
                    <Style TargetType="{x:Type DataGridRow}" BasedOn="{StaticResource ResourceKey=StyleDataGridRow}">
                        <Setter Property="Height" Value="30"/>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource ResourceKey=StyleDataGridColumnHeader}">
                        <Setter Property="Height" Value="30"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.Columns>

                    <DataGridTemplateColumn Header="操作" Width="70">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Path=OpType, Converter={StaticResource ResourceKey=FsOperationTypeNameConverter}}" Style="{StaticResource ResourceKey=StyleTextBlockLocal}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="源路径" Width="1*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Path=SourceFullPath}" Style="{StaticResource ResourceKey=StyleTextBlockLocal}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="目标路径" Width="1*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Path=TargetFullPath}" Style="{StaticResource ResourceKey=StyleTextBlockLocal}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="速度" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Path=Speed}" HorizontalAlignment="Center" Style="{StaticResource ResourceKey=StyleTextBlockLocal}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="进度" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid>
                                    <ProgressBar Minimum="0" Maximum="100" Height="20" Value="{Binding Path=Progress}"/>
                                    <TextBlock Text="{Binding Path=Progress, StringFormat='{}{0}%'}" HorizontalAlignment="Center" Style="{StaticResource ResourceKey=StyleTextBlockLocal}"/>
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="状态" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Path=State, Converter={StaticResource ResourceKey=ProcessStatesNameConverter}}" HorizontalAlignment="Center" Style="{StaticResource ResourceKey=StyleTextBlockLocal}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>

            </DataGrid>
        </Grid>

    </Grid>
</UserControl>
