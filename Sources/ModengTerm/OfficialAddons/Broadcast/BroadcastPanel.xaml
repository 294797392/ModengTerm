<mac:TabedSidePanel x:Class="ModengTerm.OfficialAddons.Broadcast.BroadcastPanel"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                    xmlns:mac="clr-namespace:ModengTerm.Addon.Controls;assembly=ModengTerm.Addon"
                    mc:Ignorable="d" 
                    d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
    </UserControl.Resources>

    <Grid Width="300">

        <DataGrid x:Name="DataGridBroadcastList" ItemsSource="{Binding}" ColumnHeaderHeight="20" RowHeight="30" Style="{StaticResource ResourceKey=StyleDataGrid}">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="会话" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Path=Name}" VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="同步输入" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox VerticalAlignment="Center" IsChecked="{Binding Path=IsChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</mac:TabedSidePanel>
