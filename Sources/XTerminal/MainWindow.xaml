<Window x:Class="XTerminal.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:xtermrendering="clr-namespace:XTerminal.Rendering"
        mc:Ignorable="d"
        Title="MainWindow" Height="768" Width="1024" WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="30"/>
        </Grid.ColumnDefinitions>

        <xtermrendering:XDocumentPanel Grid.Row="1" Grid.Column="0" x:Name="DocumentPanel">
        </xtermrendering:XDocumentPanel>

        <!-- Slider模拟滚动条 -->
        <Slider Grid.Row="1" Grid.Column="1" x:Name="SliderScrolbar" Interval="1" IsSnapToTickEnabled="True" Minimum="0" Maximum="0" Delay="500">
            <Slider.LayoutTransform>
                <RotateTransform Angle="90"/>
            </Slider.LayoutTransform>
        </Slider>
    </Grid>

</Window>