<Window x:Class="VideoTerminal.Windows.StartupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VideoTerminal.Windows"
        xmlns:viewmodels="clr-namespace:VideoTerminal.ViewModels"
        mc:Ignorable="d"
        Title="StartupWindow" Height="450" Width="800">

    <Window.DataContext>
        <viewmodels:StartupWindowVM/>
    </Window.DataContext>

    <Grid>
        <ListBox DataContext="{Binding Path=SessionList}" ItemsSource="{Binding}" SelectedItem="{Binding Path=SelectedItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Path=Name}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
